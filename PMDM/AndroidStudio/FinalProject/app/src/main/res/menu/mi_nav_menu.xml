<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/id_web"
            android:icon="@drawable/globalinternetvector"
            android:iconTint="@color/black"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/id_products"
            android:icon="@drawable/pokeballvector"
            android:iconTint="@color/black"
            android:title="@string/menu_products" />
        <item
            android:id="@+id/id_cart"
            android:icon="@drawable/cartvector"
            android:iconTint="@color/black"
            android:title="@string/menu_cart" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/id_add"
            android:enabled="false"
            android:icon="@android:drawable/ic_menu_send"
            android:iconTint="@color/black"
            android:title="Contact Us" />
    </group>
</menu>